# TODO

## 관련 개발 문서

- [프로젝트 컨텍스트·실시간 작업 UX 통합 설계](docs/superpowers/specs/2026-07-20-project-context-and-live-ux-design.md)
- [병렬 구현 계획](docs/superpowers/plans/2026-07-20-project-context-and-live-ux.md)
- [Windows 빌드·자동 QA 증적](docs/handoffs/2026-07-20-project-context-live-ux-windows-qa.md)

## 프로젝트 컨텍스트와 전체 현황 UX

- [x] 프로젝트 목표 입력·수정
  - 프로젝트 생성·편집 화면에서 목표와 현재 집중 항목을 입력할 수 있게 한다.
  - 입력값을 프로젝트 레지스트리에 저장하고 앱 재시작 후에도 유지한다.
  - 프로젝트 홈과 전체 화면에서 목표를 확인할 수 있게 한다.
  - AI가 제안한 목표와 사용자가 확정한 목표를 구분한다.

- [x] 다음 할 일 수동 입력·관리
  - 프로젝트 홈과 태스크 보드에서 사용자가 할 일을 직접 추가할 수 있게 한다.
  - 제목, 상태, 우선순위, 기한을 입력·수정하고 완료 또는 삭제할 수 있게 한다.
  - 대화에서 자동 추출된 Task와 사용자가 직접 만든 Task의 출처를 표시한다.
  - `다음 할 일` 영역에는 실행 가능하고 완료되지 않은 작업을 우선순위대로 표시한다.

- [x] 프로젝트 메모 기능
  - 프로젝트마다 항상 접근 가능한 메모 패널을 제공한다.
  - 메모 추가, 편집, 삭제, 완료/보관 및 고정을 지원한다.
  - 기존 `NextNote` 데이터를 메모 화면에서도 조회·관리할 수 있게 한다.
  - 메모와 실행 가능한 Task는 명확히 구분하되, 메모를 Task로 전환할 수 있게 한다.

- [x] `전체` 화면에 에이전트 활동 상태 표시
  - 프로젝트별 Claude, Codex, OpenCode 등의 현재 상태를 표시한다.
  - 최소 상태는 `작업 중`, `응답 대기`, `유휴`, `오류`, `연결 끊김`으로 구분한다.
  - 단순히 프로세스가 살아 있는 상태와 실제로 작업 중인 상태를 구분한다.
  - 마지막 활동 시각, 현재 작업 또는 최근 실행, 소속 프로젝트를 함께 표시한다.
  - 상태를 클릭하면 해당 프로젝트의 에이전트 패널 또는 실행 상세로 이동한다.

- [x] `전체` 화면에 최근 사용자 질문 표시
  - 각 프로젝트에서 최근 요청한 사용자 질문을 표시한다.
  - 질문 시각과 사용한 에이전트를 함께 표시한다.
  - 질문을 클릭하면 해당 대화 기록을 열거나 이어서 대화할 수 있게 한다.
  - 민감정보가 포함된 질문은 기존 개인정보 처리 정책을 적용한다.

- [x] 터미널 상단에 최근 사용자 질문 표시
  - 에이전트 이름만 표시하는 현재 제목을 `Codex [최근 질문]`, `Claude [최근 질문]` 형태로 확장한다.
  - 각 터미널 pane에 연결된 프로젝트·에이전트·세션의 마지막 사용자 질문을 사용한다.
  - 새 질문을 보내거나 세션을 재개하면 제목을 즉시 갱신한다.
  - 긴 질문은 한 줄로 줄여 표시하고 전체 문장은 hover 또는 상세 보기에서 확인할 수 있게 한다.
  - 질문이 없을 때는 기존처럼 에이전트 이름만 표시한다.
  - 여러 프로젝트와 여러 worktree의 터미널이 서로 다른 세션 제목을 정확히 유지하게 한다.
  - 민감정보가 포함된 질문은 제목에서 마스킹하거나 표시하지 않는다.

- [x] 위키 생성 중 노드 생성 과정을 실시간으로 표시
  - `노드 제안 생성` 단계에서 현재 처리 중인 폴더 또는 워커를 한 줄씩 표시한다.
  - 노드가 생성될 때마다 노드 제목, 유형, 출처 폴더와 완료 상태를 실시간 목록에 추가한다.
  - 전체 작업 수, 완료 수, 진행 중 수, 실패 또는 재시도 수를 함께 표시한다.
  - 마지막 로그가 아닌 사용자용 상태 문구로 `생성 중`, `응답 대기`, `재연결 중`, `완료`, `실패`를 구분한다.
  - 마지막 활동 시각과 경과 시간을 표시해 실제 작업 중인지 멈춘 것인지 판단할 수 있게 한다.
  - 일정 시간 새 이벤트가 없으면 `응답 대기 중` 또는 `중단 가능성` 안내를 표시한다.
  - 상세 로그는 접어서 제공하고 기본 화면에는 노드 생성 진행 요약만 보여준다.
  - 앱을 다시 열거나 실행 이력을 선택해도 완료된 노드 진행 기록을 다시 볼 수 있게 영속화한다.

- [ ] 터미널에 복사한 내용 붙여넣기 지원
  - `Ctrl+V`, `Ctrl+Shift+V`, `Shift+Insert`와 우클릭 메뉴로 클립보드 내용을 붙여넣을 수 있게 한다.
  - 한글, 코드, 경로와 여러 줄 텍스트의 문자 및 줄바꿈을 원문 그대로 유지한다.
  - 여러 줄 입력은 bracketed paste를 지원해 각 줄이 의도치 않게 즉시 실행되지 않도록 한다.
  - 클립보드 권한 또는 읽기 실패를 조용히 무시하지 않고 사용자에게 원인을 안내한다.
  - Windows 패키징 앱의 실제 에이전트 터미널에서 복사·붙여넣기 동작을 검증한다.
  - [ ] 남은 수동 QA: packaged app에서 네 가지 제스처와 local/WSL/SSH 입력을 실제로 확인한다.

- [ ] `tmux`에서 터미널 글자 깨짐 방지
  - `tmux` 실행 전후에 한글, 박스 드로잉 문자, 이모지와 Powerline 문자가 셀 너비와 줄 맞춤을 유지하게 한다.
  - PTY의 `TERM`, true color 및 UTF-8 locale 설정을 로컬·WSL·SSH 환경별로 올바르게 전달한다.
  - xterm의 Unicode 폭 계산과 실제 사용 글꼴을 맞추고, 필요한 글리프가 없으면 진단 가능한 안내를 표시한다.
  - 창과 터미널 pane의 크기를 바꾸거나 `tmux` pane을 분할·재접속해도 문자가 겹치거나 잘리지 않게 다시 맞춘다.
  - Windows 패키징 앱에서 일반 shell과 `tmux`를 나란히 비교하는 회귀 테스트를 추가한다.
  - [ ] 남은 수동 QA: packaged app의 WSL/SSH tmux에서 glyph·색·resize·재접속을 실제로 비교한다.

- [x] 대화 속 파일 경로를 `Ctrl+클릭`으로 열기
  - 사용자 질문과 에이전트 답변에 포함된 절대 경로, 프로젝트 상대 경로와 Markdown 링크를 파일 링크로 인식한다.
  - 백틱으로 감싼 경로와 Windows·WSL 경로를 지원하고 `파일:행[:열]` 표기가 있으면 해당 위치로 이동한다.
  - 실제로 존재하며 현재 프로젝트 또는 활성 worktree에서 접근 가능한 경로만 링크로 표시한다.
  - `Ctrl+클릭`하면 대화 화면 오른쪽 미리보기 패널에서 열고, 패널을 닫거나 크기를 조절할 수 있게 한다.
  - `.md`는 렌더링된 문서, `.html`은 스크립트 실행을 제한한 미리보기, `.py`는 구문 강조와 줄 번호가 있는 코드로 표시한다.
  - 파일이 없거나 읽을 수 없으면 경로를 그대로 유지하고 실패 원인을 안내한다.
  - 공백·한글·괄호가 포함된 경로와 여러 프로젝트의 동일한 상대 경로를 정확히 처리하는 테스트를 추가한다.

## 공통 완료 조건

- [x] 모든 수동 입력 데이터는 앱 재시작 후에도 유지된다.
- [x] 자동 생성 데이터와 사용자 확정 데이터를 UI에서 구분한다.
- [x] 빈 상태, 로딩, 저장 실패 및 연결 오류 상태를 사용자에게 명확히 알린다.
- [x] 저장·수정·삭제와 주요 화면 동작에 대한 테스트를 추가한다.
